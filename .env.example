# Environment variables for 1minAI OpenAI-compatible proxy

# Required: Your 1minAI API key
ONE_MIN_AI_API_KEY=your-api-key-here

# Optional: 1minAI base URL (default: https://api.1min.ai)
# ONE_MIN_AI_BASE_URL=https://api.1min.ai

# Optional: Proxy server settings
# PROXY_HOST=0.0.0.0
# PROXY_PORT=8080

# Optional: Default model when not specified in request
# DEFAULT_1MIN_MODEL=gpt-4o-mini

# Optional: Web search settings
# DEFAULT_WEBSEARCH=false
# DEFAULT_NUM_OF_SITE=1
# DEFAULT_MAX_WORD=500

# Optional: Request handling
# REQUEST_TIMEOUT_SECS=60
# RETRIES=2

# Optional: Logging
# LOG_LEVEL=INFO
# ENABLE_REQUEST_LOGGING=false

# Optional: Additional models to list in /v1/models (comma-separated)
# EXTRA_MODELS=gpt-4o,claude-3-5-sonnet,gemini-1.5-pro
